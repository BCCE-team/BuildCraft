package buildcraft.lib.net;

/** Logical direction of a BuildCraft message, independent of loader networking enums. */
public enum BCMessageDirection {
    CLIENTBOUND,
    SERVERBOUND,
    BIDIRECTIONAL
}
